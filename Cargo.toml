[package]
name = "mt_lintocirc"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
noodles = { version = "0.68.0", features = ["bam", "core", "sam" ] }

log = "0.4"
